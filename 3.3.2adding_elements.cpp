#include <iostream>
#include <string>
#include <cctype>
#include <vector>

int main() {
    std::vector<int> v2;
    for (int i = 0; i != 100; ++i)
        v2.push_back(i); //append sequential integers to v2

    return 0;
}
